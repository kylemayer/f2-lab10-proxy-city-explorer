

require('dotenv').config();

describe('munge', () => {

    test('location data', async() => {

    const expectation = {

    };

    const input = '';

    const actual = mungeLocationResponse(input);

    expect(actual).toEqual(expectation);
    });
});